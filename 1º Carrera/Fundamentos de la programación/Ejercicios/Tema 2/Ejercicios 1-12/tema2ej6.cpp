#include <iostream>
using namespace std;



int main()
{
	double a = 1, b = 1, c = 1, r = 1, d = 1, aux;
	aux = 4 / (3 * (r + 34)) - 9 * (a + b * c) + (3 + d * (2 + a)) / (a + b * d);
	cout << "Resultado: " << aux << endl;

	system("pause");
	return 0;
}